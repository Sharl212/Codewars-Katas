function duplicateEncode(word) {
    // if (duplicate == 1) {
    //     newString = word.replace(new RegExp(`${letter}`, "g"), '(')
    // }
    // if (duplicate > 1) {
    //     newString = word.replace(new RegExp(`${letter}`, "g"), ')')
    // }
}

duplicateEncode("recede") // "()()()"