/**
 * @param {string} initial
 * @param {string} goal
 * @return {number}
 */

function minimumConcat(initial, goal) {
    for (let g of goal) {
        g
        if(g)
    }
}

var initial = "xyz"
var goal = "xzyxz"

console.log(minimumConcat(initial, goal));